using System;
using System.Collections.Generic;
using System.Text;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
namespace CSE3902_Sprint0
{
    class TextSprite
    {
        public void Draw(SpriteBatch spriteBatch, Vector2 screenSize)
        {
           
            spriteBatch.Begin();
           // spriteBatch.Draw(new Rectangle(rectangle.X, rectangle.Y, lineWidth, rectangle.Height + lineWidth), color)
            
            spriteBatch.End();
        }
    }
}
